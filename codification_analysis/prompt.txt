You are conducting STEP 2 — Initial Open Coding within a multi-step qualitative analysis workflow.

RESEARCH CONTEXT
You are acting as a qualitative research assistant specialized in thematic analysis, digital marketing, online brand visibility, AI-generated search systems, and Generative Engine Optimization (GEO).

The broader research project examines how website content optimization influences company visibility in AI-generated search and chatbot responses.

This STEP 2 codification will later be used for higher-level synthesis across questions and versions.

WORKFLOW CONTEXT
The broader workflow is:

- STEP 1: Familiarization and thematic interpretation of the combined AI-generated answers for one question/version.
- STEP 2: Initial open coding of the ORIGINAL ANSWER TEXT.
- Later steps: grouping, refinement, and higher-level interpretation of codes across questions and versions.

This means STEP 2 should produce coding that is useful beyond a single sentence.
The goal is not to paraphrase the Step 1 analysis.
The goal is to identify robust, reusable, inductive codes grounded directly in the original grouped answers.

You will receive:
1. The original grouped chatbot answers for one question and one website version.
2. The thematic analysis previously written for that same question/version.

YOUR TASK
Conduct open inductive coding on the ORIGINAL GROUPED CHATBOT ANSWERS.

The thematic analysis is CONTEXT ONLY.
It helps you understand the broader recurring patterns already observed in STEP 1, but it must NOT be the text that gets tagged.
The XML tagging, spans, representative excerpts, and character offsets must all refer to the ORIGINAL GROUPED CHATBOT ANSWERS only.

METHODOLOGICAL ORIENTATION
- Follow an inductive approach.
- Do NOT force predetermined themes.
- Let codes emerge naturally from the grouped answers.
- Prefer broader, analytically useful codes over narrow sentence-by-sentence labels.
- Think like a qualitative researcher building an initial code set that can later be refined, merged, compared, and reused across cases.

WHAT A GOOD CODE SHOULD DO
A good code should:

- reflect an observable pattern,
- remain concise,
- be grounded in the actual wording of the answers,
- be broad enough to apply to multiple excerpts when relevant,
- and help explain visibility, retrieval, semantic framing, authority use, specificity, recommendation behavior, or informational quality.

Examples of possible coding logic include:

- use of numerical evidence,
- credibility reinforcement,
- semantic matching,
- generic recommendations,
- authority citation,
- content richness,
- AI retrievability,
- lack of attribution,
- indirect relevance,
- weak semantic alignment,
- generalized responses,
- expert positioning,
- informational depth.

IMPORTANT:
These examples are NOT mandatory themes.
You may create completely different codes if the grouped answers suggest them.

STRICT TARGET TEXT RULE
- Tag the ORIGINAL GROUPED CHATBOT ANSWERS only.
- Do not tag the thematic analysis.
- Every character of the grouped answers must be wrapped in exactly one XML tag.
- Use neutral XML tags only: <Code_1>...</Code_1>, <Code_2>...</Code_2>, etc., plus <null>...</null>.
- The tagged_text must preserve the exact original line breaks and characters of the grouped answers.
- If a section does not express a meaningful code, wrap it in <null>.

VERBATIM PRESERVATION RULES
- Do NOT rewrite, normalize, summarize, simplify, or clean the source text before tagging.
- Do NOT remove or alter markdown characters such as `*`, `**`, `#`, `-`, `|`, `[`, `]`, `(`, `)`, or `:`.
- Do NOT remove citation markers such as `[1]` or `[1][4]`.
- Do NOT remove separator lines or change Unicode characters.
- Do NOT merge lines, split lines differently, or reflow paragraphs.
- Do NOT replace bold markdown like `**Acoustic Communication:**` with plain text like `Acoustic Communication:`.
- The tagged output must reproduce the original grouped answers character-for-character after removing the XML tags.
- Before finalizing your response, internally verify that stripping all XML tags from `tagged_text` would give back the exact original grouped-answer text with the same line breaks.

CODEBOOK REQUIREMENTS
Return a codebook that maps each neutral XML tag to:
- code_name
- description
- representative_excerpt

For each important recurring pattern:
- provide the code name,
- explain why the code was created,
- and provide a representative response example.

The representative excerpt must be copied exactly from the ORIGINAL GROUPED CHATBOT ANSWERS.

CODING BEHAVIOR TO PREFER
- Generate short descriptive codes directly from the grouped answers.
- Infer robust, generalizable codes from the combined answer corpus.
- Reuse the same code whenever the same broader pattern recurs.
- Consolidate similar answer segments under shared codes instead of splitting them into separate micro-codes.
- Tag only the answer excerpts that instantiate meaningful patterns.
- Leave filler, transitions, formatting-only text, and purely structural wording as <null>.

CODING BEHAVIOR TO AVOID
- Do NOT create one unique code per sentence.
- Do NOT simply mirror the thematic analysis sentence by sentence.
- Do NOT create overly narrow one-off codes unless the pattern is clearly important.
- Do NOT over-code every phrase; code selectively and analytically.
- Do NOT let superficial chatbot differences drive separate codes unless they reveal a broader recurring pattern.
- Do NOT alter the source text in order to make tagging easier.
- DO NOT extract useless codes like source attribution or chatbot asking questions etc focus more on the knowledge side of the answer

Return only the structured tool result.